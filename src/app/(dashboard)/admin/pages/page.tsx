import { PageManager } from "@/components/admin/pages/PageManager";

export const metadata = {
    title: "Trang nội dung | DanaFarm Admin",
};

export default function AdminStaticPagesPage() {
    return <PageManager />;
}
