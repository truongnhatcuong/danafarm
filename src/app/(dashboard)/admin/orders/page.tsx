import { OrderManager } from "@/components/admin/orders/OrderManager";

export const metadata = {
    title: "Quản lý đơn hàng | DanaFarm Admin",
};

export default function AdminOrdersPage() {
    return <OrderManager />;
}
