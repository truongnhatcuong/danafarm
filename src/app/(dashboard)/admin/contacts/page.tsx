import { ContactManager } from "@/components/admin/contacts/ContactManager";

export const metadata = {
    title: "Tin nhắn liên hệ | DanaFarm Admin",
};

export default function AdminContactsPage() {
    return <ContactManager />;
}
