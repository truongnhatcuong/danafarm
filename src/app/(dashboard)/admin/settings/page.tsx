import { SettingsManager } from "@/components/admin/settings/SettingsManager";

export const metadata = {
    title: "Cài đặt website | DanaFarm Admin",
};

export default function AdminSettingsPage() {
    return <SettingsManager />;
}
